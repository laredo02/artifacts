#include <iostream>
#include <fstream>

using namespace std;

int main () {
  ofstream image;
  image.open("image.ppm");
  if (image.is_open()) {
    image << "p3" << endl;
    image << "250 250" << endl;
    image << "250" << endl;
  }
  image.close();
}
